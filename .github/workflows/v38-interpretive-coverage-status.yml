name: V3.8 interpretive coverage status

on:
  push:
    branches:
      - preview/alpha-productization-px01
    paths:
      - .github/workflows/v38-interpretive-coverage-status.yml
      - scripts/v38-add-interpretive-coverage-status.py

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: preview/alpha-productization-px01
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - name: Apply V3.8 coverage status UI
        run: python scripts/v38-add-interpretive-coverage-status.py
      - name: Format
        run: bunx prettier --write 'src/routes/_authenticated/blueprints.$blueprintId.tsx'
      - name: Tests
        run: bun test
      - name: Typecheck
        run: bunx tsc --noEmit
      - name: Lint
        run: bun run lint
      - name: Build
        run: bun run build
      - name: Commit V3.8
        run: |
          if git diff --quiet; then
            echo "No V3.8 changes needed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- 'src/routes/_authenticated/blueprints.$blueprintId.tsx'
          git commit -m 'feat(blueprints): classify interpretive coverage by step'
          git push
