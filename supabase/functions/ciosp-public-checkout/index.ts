import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2";
const SUPABASE_URL=Deno.env.get("SUPABASE_URL")!;const P=JSON.parse(Deno.env.get("SUPABASE_PUBLISHABLE_KEYS")??"{}");const S=JSON.parse(Deno.env.get("SUPABASE_SECRET_KEYS")??"{}");const publishableKey=P.default,secretKey=S.default;const MP_ENVIRONMENT=Deno.env.get("MERCADO_PAGO_ENVIRONMENT")??"test";const CODE="CIOSP-SP-2027";
const cors={"access-control-allow-origin":"*","access-control-allow-headers":"authorization, content-type, x-client-info, apikey, x-ciosp-qa","access-control-allow-methods":"POST, OPTIONS"};const json=(b:unknown,s=200)=>new Response(JSON.stringify(b),{status:s,headers:{...cors,"content-type":"application/json; charset=utf-8"}});const hex=(b:ArrayBuffer)=>[...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,"0")).join("");async function sha256(v:string){return hex(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(v)))}function phone(v?:string){if(!v)return null;const r=v.trim(),d=r.replace(/\D/g,"");if(!r)return null;if(r.startsWith("+")&&d.length>=8&&d.length<=15)return`+${d}`;if(d.length===10||d.length===11)return`+55${d}`;return r}function qaRef(req:Request){const r=req.headers.get("referer");if(!r)return false;try{const u=new URL(r);return u.pathname==="/ciosp-2027"&&u.searchParams.get("sales_qa")==="1"}catch{return false}}
Deno.serve(async(req:Request)=>{if(req.method==="OPTIONS")return new Response(null,{status:204,headers:cors});if(req.method!=="POST")return json({error:"method_not_allowed"},405);if(!secretKey)return json({error:"server_not_configured"},500);let b:any;try{b=await req.json()}catch{return json({error:"invalid_json"},400)}const full=(b.full_name??"").trim(),email=(b.email??"").trim().toLowerCase(),ph=phone(b.phone),idem=(b.idempotency_key??crypto.randomUUID()).trim();if(full.length<2||full.length>120)return json({error:"invalid_full_name"},400);if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)||email.length>254)return json({error:"invalid_email"},400);if(ph&&!/^\+[1-9][0-9]{7,14}$/.test(ph))return json({error:"invalid_phone"},400);if(idem.length<16||idem.length>120)return json({error:"invalid_idempotency_key"},400);const admin=createClient(SUPABASE_URL,secretKey,{auth:{persistSession:false}});const{data:op,error:oe}=await admin.from("operations").select("id,tenant_id,code,offering_id,archived_at").eq("code",CODE).is("archived_at",null).maybeSingle();if(oe)return json({error:"operation_lookup_failed"},500);if(!op?.offering_id)return json({error:"checkout_not_configured"},409);const{data:offering,error:ofe}=await admin.from("offerings").select("id,status,metadata").eq("id",op.offering_id).eq("tenant_id",op.tenant_id).maybeSingle();if(ofe)return json({error:"offering_lookup_failed"},500);if(!offering)return json({error:"offering_not_found"},404);const meta=offering.metadata??{};let allow=false;const closed=offering.status!=="active"||meta.sales_public!==true;if(closed){const intent=req.headers.get("x-ciosp-qa")==="1"||qaRef(req);if(!intent)return json({error:"sales_not_open"},409);if(MP_ENVIRONMENT!=="test")return json({error:"qa_checkout_test_only"},503);if(!publishableKey)return json({error:"qa_auth_not_configured"},500);const ah=req.headers.get("authorization");if(!ah)return json({error:"qa_auth_required"},401);const uc=createClient(SUPABASE_URL,publishableKey,{global:{headers:{Authorization:ah}},auth:{persistSession:false}});const{data:ad,error:ae}=await uc.auth.getUser();if(ae||!ad.user)return json({error:"qa_invalid_session"},401);const{data:m,error:me}=await admin.from("memberships").select("role,status").eq("tenant_id",op.tenant_id).eq("profile_id",ad.user.id).eq("status","active").maybeSingle();if(me)return json({error:"qa_membership_check_failed"},500);if(!m||!["owner","admin","operations_agent"].includes(m.role))return json({error:"qa_forbidden"},403);allow=true}const{data:sell}=await admin.from("sellables").select("id").eq("tenant_id",op.tenant_id).eq("offering_id",offering.id).eq("status","active").limit(1).maybeSingle();if(!sell)return json({error:"active_sellable_not_found"},409);const now=new Date().toISOString();const{data:price}=await admin.from("prices").select("id").eq("tenant_id",op.tenant_id).eq("sellable_id",sell.id).eq("status","active").lte("valid_from",now).or(`valid_until.is.null,valid_until.gt.${now}`).limit(1).maybeSingle();if(!price)return json({error:"active_price_not_found"},409);const bytes=crypto.getRandomValues(new Uint8Array(32)),token=[...bytes].map(x=>x.toString(16).padStart(2,"0")).join(""),hash=await sha256(token);const{data,error}=await admin.rpc("create_public_checkout_order",{_operation_code:CODE,_full_name:full,_email:email,_phone_e164:ph,_checkout_token_hash:hash,_idempotency_key:idem,_allow_closed:allow});if(error)return json({error:"checkout_order_failed",details:error.message},409);if(data?.reused===true&&data?.session_id&&data?.order_id){const{data:rot,error:re}=await admin.from("public_checkout_sessions").update({token_hash:hash,status:"active",expires_at:new Date(Date.now()+7200000).toISOString()}).eq("id",data.session_id).eq("order_id",data.order_id).eq("tenant_id",op.tenant_id).select("id").maybeSingle();if(re||!rot)return json({error:"checkout_session_rotation_failed"},409)}return json({...data,checkout_token:token,payer_email:email,checkout_key:"commercial",qa_mode:allow},201)});