# Release note

Adds a governed backend capability for operators to notify travelers about confirmed schedule, meeting-point/location and delay changes using existing COBS in-app communications, with operation isolation and idempotent replay.