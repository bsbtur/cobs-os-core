# Data safety

The publisher stores the canonical source kind/id and alert type in message metadata. It does not expose provider credentials, payment data or AI context. Recipient resolution is delegated to the existing W08 operation-scoped communication functions.