# Audience isolation

The publisher creates an `all_participations` selector on the target message, then delegates resolution and in-app eligibility to W08 using the same tenant_id and operation_id. Controlled QA on the CIOSP traveler operation resolved only that operation's audience.