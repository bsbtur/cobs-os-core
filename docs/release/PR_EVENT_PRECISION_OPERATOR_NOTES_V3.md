# Event precision operator control status

Status: implementation in progress on isolated branch.

The current control uses the approved W07 command and idempotency key. It is intentionally not merge-ready until connected to the existing Events header and validated through CI, preview and authenticated QA.
