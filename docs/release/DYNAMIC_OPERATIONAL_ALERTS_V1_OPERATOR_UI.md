# Operator UI next gate

Add an explicit communication choice after a canonical journey/transport update succeeds. The UI should preview title/body and alert type, then invoke the governed publisher. Never auto-notify from generic edit/save.