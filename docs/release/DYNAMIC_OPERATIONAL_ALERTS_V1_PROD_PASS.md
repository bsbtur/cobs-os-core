# Production PASS

Supabase production accepts authenticated operator publication, rejects grant-only traveler and null-auth publication, resolves operation audience, creates in-app delivery, and replays idempotently. Controlled test rows were rolled back.