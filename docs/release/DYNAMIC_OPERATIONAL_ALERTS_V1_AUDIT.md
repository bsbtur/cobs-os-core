# Audit

Successful new publication records `operations.dynamic_alert_published` with operation, alert type, canonical source and recipient/reachability counts. Replays return the existing message rather than producing a second publication.