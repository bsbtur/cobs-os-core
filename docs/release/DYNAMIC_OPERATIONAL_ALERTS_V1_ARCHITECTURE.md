# Architecture

Confirmed operational fact → explicit operator publication → `publish_dynamic_operational_alert` → existing W08 audience resolution → `messages` → `message_recipients` → in-app delivery. Administrative edits alone do not notify travelers.