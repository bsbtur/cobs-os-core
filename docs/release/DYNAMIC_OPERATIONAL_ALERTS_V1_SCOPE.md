# V1 scope

Dynamic Operational Alerts V1 intentionally supports only explicit `time_changed`, `location_changed`, and `delay` publications. Administrative edits are not automatically converted into traveler communication. No second n8n workflow is introduced.