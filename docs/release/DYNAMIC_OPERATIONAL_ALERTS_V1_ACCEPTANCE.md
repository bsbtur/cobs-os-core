# Acceptance

Backend gate is accepted when: operator can publish an explicit confirmed fact; only operation audience resolves; reachable travelers receive in-app delivery; replay is idempotent; traveler/anonymous publication is denied; no test artifacts persist. All backend criteria passed in controlled production transactions.