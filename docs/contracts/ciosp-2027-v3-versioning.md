# Versioning

A signed or signable contract always references an immutable `template_key + template_version`. V3 is a new version rather than an edit of V1, preserving historical evidence and reproducibility.
