# Audit evidence

Generation stores actor, timestamp, canonical order snapshot, customer contractual profile, operation, reservation, template version and contract variables. A deterministic `created` event is appended. Provider evidence is added only in later provider-specific stages.
