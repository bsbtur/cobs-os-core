# Contracts

Canonical contract text is versioned in COBS. Never overwrite a version that may already be referenced by a customer contract. New legal/commercial wording receives a new template version. Signature providers are delivery/evidence integrations, not the source of truth for contractual content.
