# Provider document mode

The V3 registry uses `provider_document_mode=upload`. The canonical rendered contract remains a COBS artifact; the signature adapter may upload PDF/DOCX to the provider. This avoids making provider templates the source of contractual truth.
